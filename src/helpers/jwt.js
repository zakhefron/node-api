class Jwt {
  /**
   * Generate a jwt secret token for user.
   *
   * @param {*} data
   */
  generateToken(data) {}

  /**
   * Decode a provided jwt token.
   *
   * @param {string} token
   */
  decodeJwtToken(token) {}
}
